# Event-discoverer

## Installation 

Install packages for the app
```bash
cd app && npm install
```

Install packages for the api
```bash
cd api && npm install
```

## Running 

```bash
cd api && npm start
cd app && npm start
```